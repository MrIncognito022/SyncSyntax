<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" asp-action="Index">SyncSyntax</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a asp-action ="Index" asp-controller="Post"  class="nav-link">Home</a>
                </li>
                <li class="nav-item ">
                    <a asp-action="Index" class="nav-link">About</a>
                </li>
                <li class="nav-item ">
                    <a asp-action="Index" class="nav-link">Contact</a>
                </li>
                @if(User.IsInRole("Admin")){
                    <li class="nav-item">
                        <a class="nav-link" asp-action="Index" asp-controller="Category">Category</a>
                    </li>
                }
            </ul>
            
            @if (User.Identity.IsAuthenticated)
            {
                <a asp-controller="Auth" asp-action="SignOut" class="btn btn-outline-light">SignOut</a>
            }
            else
            {
                 <div class="d-flex ms-auto gap-2">
                    <a asp-controller="Auth" asp-action="SignIn" class="btn btn-outline-light">SignIn</a>
                    <a asp-controller="Auth" asp-action="SignUp" class="btn btn-outline-light">SignUp</a>
                </div>
            }
            
        </div>
    </div>
</nav>